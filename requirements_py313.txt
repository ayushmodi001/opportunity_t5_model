# Python 3.13 Compatible Requirements
# Install these packages one by one using: pip install <package_name>

# Core web framework
fastapi>=0.100.0
uvicorn>=0.20.0

# Configuration and utilities
python-dotenv>=1.0.0
requests>=2.28.0

# AI/ML (install these manually if they fail)
mistralai>=0.4.0

# Basic NLP (lightweight alternatives)
yake>=0.4.8

# Optional: Heavy ML packages (skip if they cause build errors)
# transformers>=4.30.0
# torch>=2.0.0
# spacy>=3.6.0
